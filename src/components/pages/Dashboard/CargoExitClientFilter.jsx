import { useState, useEffect } from "react";
import PropTypes from "prop-types";
import { Button, Card, Form, InputGroup } from "react-bootstrap";

export const CargoExitClientFilter = () => {
  return "Hello";
};
